const assert = require('assert');
const { oddNums } = require('../lib/array');

describe('array method', () => {

    describe('square of odd nums', () => {
        it('squares the odd numbers in the array', () => {
            const numbers = [2, 3, 9, 12, 8, 5, 7];
            // const squared = numbers;
            function oddNums(num) {
                return num === (num % 2 === 0);
                
            }
        });    
    });
});